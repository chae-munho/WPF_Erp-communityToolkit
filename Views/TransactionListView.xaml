<UserControl x:Class="WpfApp1.Views.TransactionListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="350" d:DesignWidth="400">

    <GroupBox Header="입출고 내역">
        <Grid Margin="10">
            <DataGrid ItemsSource="{Binding Transactions}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="시간" Binding="{Binding Time}" Width="160"/>
                    <DataGridTextColumn Header="제품" Binding="{Binding ProductName}" Width="*"/>
                    <DataGridTextColumn Header="구분" Binding="{Binding Type}" Width="80"/>
                    <DataGridTextColumn Header="수량" Binding="{Binding Quantity}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </GroupBox>
</UserControl>
